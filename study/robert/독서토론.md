# 📖 ROS 2 입문 독서토론 (1~8장)

> **진행 형식**: 각 토픽별 5~10분 자유 토론  
> **대상**: ROS 2 입문 스터디 참가자  
> **범위**: 1장 ROS 2 소개 ~ 8장 DDS의 QoS

---

## 1장: ROS 2 소개

### 토픽 1. ROS 2는 "운영체제"인가, "프레임워크"인가?
ROS 2의 이름에는 Operating System이 포함되어 있지만, 실제로는 미들웨어 기반 플랫폼이라고 설명합니다.  
- 왜 "운영체제"라는 이름이 붙었을까요?
- 이 이름이 초보자에게 혼란을 줄 수 있다면, 어떤 이름이 더 적절할까요?

> **💬 보편적 답변**  
> ROS는 초기에 프로세스 관리, 하드웨어 추상화, 메시지 전달 등 OS와 유사한 역할을 하기 때문에 이 이름이 붙었습니다. 하지만 Linux 위에서 동작하는 미들웨어이므로 "로봇 개발 프레임워크" 또는 "로봇 미들웨어 플랫폼"이 실체에 더 가깝습니다. 이름 때문에 "Ubuntu 대신 ROS를 설치하는 건가?"라고 오해하는 입문자가 실제로 많습니다.

---

### 토픽 2. DDS 기반 통신 vs 중앙 마스터 방식
ROS 1은 ROS Master 중심, ROS 2는 DDS 기반 분산 통신을 사용합니다.  
- 중앙 마스터 방식이 더 유리한 상황은 없을까요?
- 분산 통신으로 전환하면서 새로 생기는 어려움은 무엇일까요?

> **💬 보편적 답변**  
> 중앙 마스터 방식은 구조가 단순해서 소규모 단일 로봇·교육 환경에서는 이해하기 쉽다는 장점이 있습니다. 반면 DDS 분산 통신은 Discovery 과정에서 네트워크 트래픽이 증가하고, QoS 설정이 추가로 필요해 초기 학습 곡선이 높아집니다. 하지만 멀티 로봇·산업 환경에서는 단일 장애 지점이 사라지므로 DDS가 압도적으로 유리합니다.

---

### 토픽 3. Topic / Service / Action, 언제 무엇을 쓸까?
ROS 2는 세 가지 통신 방식을 제공합니다.  
- 실제 로봇 프로젝트에서 각각 어떤 상황에 적합한지 예시를 들어볼까요?
- 하나의 통신 방식만으로 모든 것을 해결할 수 없는 이유는 무엇일까요?

> **💬 보편적 답변**  
> **Topic**은 센서 데이터처럼 지속적으로 흐르는 데이터(카메라 이미지, 라이다 스캔)에 적합합니다. **Service**는 "현재 좌표 알려줘"처럼 즉시 요청-응답이 필요한 경우에 사용합니다. **Action**은 "목표 지점까지 이동"처럼 시간이 걸리고 중간 진행률 피드백이 필요한 작업에 적합합니다. 로봇 시스템에는 이 세 가지 패턴이 모두 동시에 존재하기 때문에 하나로 통일할 수 없습니다.

---

## 2장: ROS 2 기반 로봇 개발에 필요한 정보

### 토픽 4. 사전 지식의 범위 — 어디까지 알아야 시작할 수 있을까?
Linux, Python/C++, 네트워크, 센서, 좌표계 등 많은 사전 지식이 필요하다고 합니다.  
- 이 모든 것을 알아야 ROS 2를 시작할 수 있을까요?
- 최소한의 "시작 가능 수준"은 어디까지라고 생각하나요?

> **💬 보편적 답변**  
> 모든 사전 지식을 완벽히 갖추고 시작할 필요는 없습니다. 최소한 **Linux 터미널 기본 조작**(cd, ls, mkdir)과 **Python 기초 문법**(변수, 함수, 클래스)만 알면 turtlesim 예제부터 시작할 수 있습니다. 나머지(좌표계, 네트워크, 센서)는 실습하면서 필요할 때 채워가는 방식이 현실적이고 효율적입니다.

---

### 토픽 5. Python으로 시작 vs C++로 시작
책에서는 Python으로 시작해 C++로 확장하는 방식을 권장합니다.  
- Arduino 경험이 있는 사람은 C++부터 시작하는 것이 더 좋을까요?
- 두 언어를 동시에 배우면 혼란스러울까요, 시너지가 날까요?

> **💬 보편적 답변**  
> Arduino C++과 ROS 2 C++(rclcpp)은 문법은 같지만 구조가 상당히 다릅니다(콜백, 스마트 포인터, CMake 등). 따라서 Arduino 경험이 있더라도 ROS 2의 노드·통신 개념을 **Python으로 먼저 빠르게 익힌 뒤** C++로 전환하는 것이 대부분의 학습자에게 효율적입니다. 두 언어를 동시에 배우면 ROS 2 개념과 언어 문법이 섞여 부담이 커질 수 있으므로 순차적 접근을 권장합니다.

---

### 토픽 6. 좌표계(TF)는 왜 어려운가?
`map`, `odom`, `base_link` 등 다양한 좌표계가 등장합니다.  
- 좌표계를 처음부터 깊이 이해해야 할까요, 아니면 실습하면서 익혀도 될까요?
- 좌표계 개념이 중요한 실제 사례를 들어볼 수 있나요?

> **💬 보편적 답변**  
> 좌표계는 처음부터 완벽히 이해하려 하면 진입 장벽이 높아집니다. "base_link는 로봇 자체, odom은 출발점 기준 누적 위치, map은 전체 지도 기준" 정도만 우선 이해하고, **RViz2에서 TF 트리를 시각화하며 실습**하면 자연스럽게 체감됩니다. 실제 사례로는 라이다 센서가 로봇 위에 장착되었을 때, 센서 좌표(laser)를 로봇 좌표(base_link)로 변환해야 장애물 위치를 정확히 계산할 수 있는 상황이 대표적입니다.

---

## 3장: ROS 2 개발환경 구축

### 토픽 7. 네이티브 설치 vs Docker vs 가상머신
세 가지 설치 방식이 소개되었습니다.  
- 각자의 학습 환경에서는 어떤 방식이 가장 현실적인가요?
- Windows 사용자가 ROS 2를 학습할 때 가장 좋은 방법은 무엇일까요?

> **💬 보편적 답변**  
> **입문자에게는 Ubuntu 네이티브 설치가 가장 무난합니다.** 문제 발생 시 검색 가능한 자료가 가장 많기 때문입니다. Windows 사용자는 **WSL2 + Ubuntu**가 현실적인 대안이며, Docker는 환경 격리에 좋지만 GUI 도구(RViz2, Gazebo) 연결이 번거로울 수 있습니다. 가상머신은 작동하지만 성능 저하가 있어 시뮬레이션이 느려질 수 있습니다.

---

### 토픽 8. 환경 변수 실수 — 흔한 초기 오류의 공통점
`source` 미적용, 패키지 인식 실패 등 초기 오류가 자주 언급됩니다.  
- 환경 변수 관련 실수를 줄이기 위한 자신만의 방법이 있나요?
- 초보자에게 가장 먼저 알려줘야 할 "생존 팁"은 무엇일까요?

> **💬 보편적 답변**  
> 가장 확실한 방법은 **`.bashrc` 파일에 `source` 명령을 등록해 터미널 실행 시 자동 적용**되게 하는 것입니다. 초보자 생존 팁 3가지: ① 빌드 후 반드시 `source install/setup.bash` 실행 ② 에러 발생 시 `ros2 node list` / `ros2 topic list`로 현재 상태부터 확인 ③ "안 되면 새 터미널 열고 source부터 다시" — 이 습관만으로도 초기 오류의 절반 이상을 해결할 수 있습니다.

---

## 4장: 왜? ROS 2로 가야 하는가

### 토픽 9. ROS 2는 정말 "산업 현장"에 준비되었나?
책에서는 ROS 2가 산업 적용에 유리하다고 설명합니다.  
- 실제 산업 현장에서 ROS 2를 도입할 때 예상되는 장벽은 무엇일까요?
- ROS 2가 아직 부족한 영역이 있다면 어떤 부분일까요?

> **💬 보편적 답변**  
> ROS 2는 구조적으로 산업에 적합하게 설계되었지만, 실제 도입 시에는 **실시간 제어의 엄격한 보장**(hard real-time), **장기 운영 안정성 검증**, **기존 PLC/산업 프로토콜과의 통합**, **사내 인력의 ROS 2 숙련도 확보**가 장벽이 될 수 있습니다. 또한 DDS 벤더 선택, 보안 인증, 상용 기술 지원 부분은 아직 발전 중인 영역입니다. 그럼에도 물류 로봇, 서비스 로봇 분야에서는 이미 적극적으로 도입되고 있습니다.

---

### 토픽 10. ROS 2를 배우는 것이 "나의 커리어"에 어떤 의미인가?
ROS 2는 학습자에게 장기적 역량이 된다고 합니다.  
- ROS 2 역량이 실제 취업·창업·연구에서 어떤 가치를 가질까요?
- 로봇 개발자가 아닌 사람에게도 ROS 2 학습이 의미가 있을까요?

> **💬 보편적 답변**  
> ROS 2 역량은 로봇 스타트업, 자율주행 기업, 제조 자동화 회사 등에서 **채용 시 우대하는 핵심 기술**로 자리 잡고 있습니다. 연구자에게는 논문 구현과 재현의 표준 도구이며, 창업자에게는 빠른 프로토타이핑 수단입니다. 로봇 개발자가 아니더라도 **분산 시스템 설계, 모듈화, 비동기 통신** 등 소프트웨어 아키텍처 역량을 키울 수 있어 간접적 가치가 있습니다. 다만 투자 대비 효과는 로봇·임베디드·IoT 분야에 관심이 있는 사람에게 가장 큽니다.

---

## 5장: ROS 2의 중요 콘셉트

### 토픽 11. 노드(Node) 하나에 하나의 기능 — 현실적인가?
로봇 시스템에서 노드를 잘게 나누면 유지보수에는 유리하지만, 오버헤드가 생길 수도 있습니다.  
- 하나의 노드에 여러 기능을 넣어야 유리한 상황이 있을까요?
- 실제 프로젝트에서 노드 분리 기준을 어떻게 정하면 좋을까요?

> **💬 보편적 답변**  
> 원칙적으로 단일 책임 원칙을 따르는 것이 유지보수와 디버깅에 유리합니다. 다만 **지연 시간이 매우 중요한 제어 루프**나 **마이크로컨트롤러처럼 자원이 제한된 환경**에서는 여러 기능을 하나의 노드에 합치는 것이 현실적입니다. 노드 분리 기준으로는 "독립적으로 재시작하거나 교체할 수 있는 단위인가?"를 기준으로 삼으면 좋습니다. 예: 카메라 드라이버 노드와 이미지 처리 노드는 분리, 동일 제어 루프 내 상태 추정과 명령 발행은 합칠 수 있습니다.

---

### 토픽 12. Topic / Service / Action 중 가장 많이 쓰이는 것은?
세 가지 통신 방식은 각자 역할이 다릅니다.  
- 실제 로봇 소프트웨어에서 가장 빈번하게 사용되는 통신 방식은 무엇일까요?
- Action을 쓰지 않고 Service만으로 동일한 기능을 구현하면 어떤 문제가 생길까요?

> **💬 보편적 답변**  
> 실제 시스템에서는 **Topic이 가장 많이 사용됩니다.** 센서 데이터, 제어 명령, 상태 정보 등 대부분이 연속 스트림이기 때문입니다. Service는 설정 변경이나 단발성 조회에 사용됩니다. Action 없이 Service만으로 장기 작업을 구현하면 응답을 기다리는 동안 클라이언트가 블로킹되고, 취소·피드백 처리를 직접 구현해야 해 코드가 복잡해집니다. 즉, Action은 "긴 작업의 표준화된 패턴"이라고 이해하면 됩니다.

---

### 토픽 13. 런치 파일(Launch File)이 중요한 이유는 무엇인가?
런치 파일은 여러 노드를 한 번에 실행합니다.  
- 런치 파일 없이 노드를 하나씩 실행하면 어떤 불편함이 생길까요?
- Python 기반 런치 파일의 가장 큰 장점은 무엇이라고 생각하나요?

> **💬 보편적 답변**  
> 실제 로봇 시스템에는 수십 개의 노드가 동작합니다. 하나씩 실행하면 터미널을 수십 개 열어야 하고, 순서 관리도 직접 해야 합니다. 런치 파일은 이 모든 것을 코드 한 줄로 해결합니다. Python 기반의 가장 큰 장점은 **조건문과 변수로 유연한 실행 분기가 가능하다**는 것입니다. 예를 들어 시뮬레이션 모드/실제 하드웨어 모드를 하나의 런치 파일에서 플래그 하나로 전환할 수 있습니다.

---

## 6장: ROS 1과 2의 차이점으로 알아보는 ROS 2의 특징

### 토픽 14. ROS 1에서 ROS 2로 전환, 얼마나 어려운가?
ROS 1 경험자가 ROS 2로 넘어올 때 가장 큰 변화는 무엇일까요?  
- 기존 ROS 1 코드를 ROS 2로 마이그레이션할 때 가장 어려운 부분은 무엇일까요?
- ROS 1 없이 처음부터 ROS 2를 배우는 것이 유리할까요?

> **💬 보편적 답변**  
> ROS 1 경험자의 경우 **rosmaster 없는 분산 구조**, **콜백·실행자(Executor) 모델**, **colcon 빌드 시스템**, **QoS 개념** 추가가 주요 변화입니다. 마이그레이션 시 가장 어려운 부분은 메시지 타입 변경, 런치 파일 XML→Python 전환, 그리고 노드 생명주기 관리입니다. 처음부터 ROS 2를 배우는 사람이 오히려 ROS 1의 낡은 습관 없이 깔끔하게 배울 수 있어 장기적으로 유리하다는 의견도 많습니다.

---

### 토픽 15. 노드 생명주기(Lifecycle Node)는 언제 필요한가?
ROS 2는 노드의 상태(Unconfigured → Active → Finalized)를 명시적으로 관리합니다.  
- 생명주기 관리가 없는 일반 노드와 어떻게 다른가요?
- 어떤 시스템에서 생명주기 노드가 특히 중요할까요?

> **💬 보편적 답변**  
> 일반 노드는 시작하면 바로 동작하지만, 생명주기 노드는 **설정(configure) → 활성화(activate)** 단계를 거쳐야 실제로 동작합니다. 이를 통해 모든 노드가 준비된 후 일제히 활성화하거나, 특정 노드만 중지하고 재설정하는 것이 가능합니다. 안전이 중요한 **산업용 로봇, 의료 로봇, 자율주행 시스템**에서 특히 중요합니다. 예를 들어 센서 노드가 아직 캘리브레이션 중일 때 제어 노드가 먼저 활성화되는 문제를 방지할 수 있습니다.

---

### 토픽 16. ROS_DOMAIN_ID로 멀티 로봇을 분리하는 방식의 한계는?
ROS 2는 도메인 ID를 통해 로봇 그룹을 분리합니다.  
- 같은 네트워크에 여러 팀이 동시에 ROS 2를 실습할 때 어떤 문제가 생길 수 있나요?
- 도메인 분리만으로 충분한 격리가 될까요?

> **💬 보편적 답변**  
> 교육 환경에서 여러 팀이 같은 Wi-Fi를 쓰면, 누군가 `ROS_DOMAIN_ID`를 같은 값으로 설정하면 서로의 토픽이 보이거나 충돌이 발생합니다. 도메인 ID만으로는 **암호화나 인증이 없어** 보안 격리가 완전하지 않습니다. 완전한 격리를 원한다면 네트워크 서브넷 분리 또는 DDS-Security를 적용해야 합니다. 실습 환경에서는 팀별로 도메인 ID를 사전에 배분(팀1=10, 팀2=20 등)하는 것이 현실적인 해결책입니다.

---

## 7장: ROS 2와 DDS

### 토픽 17. DDS를 직접 쓰지 않는데, 왜 알아야 할까?
ROS 2는 RMW를 통해 DDS를 추상화하므로 개발자가 DDS를 직접 다룰 일은 거의 없습니다.  
- 그럼에도 DDS 내부 동작을 이해하는 것이 왜 중요할까요?
- DDS를 모르면 어떤 상황에서 문제가 생길 수 있을까요?

> **💬 보편적 답변**  
> DDS를 모르면 **토픽이 연결되지 않거나(QoS 불일치), 다른 서브넷의 노드가 보이지 않거나, 네트워크 트래픽이 과도하게 발생하는** 문제를 만났을 때 원인을 파악하기 어렵습니다. 예를 들어 퍼블리셔와 서브스크라이버가 실행 중임에도 데이터가 전달되지 않을 때, QoS 호환성 문제인지 Discovery 문제인지 판단하려면 DDS 개념이 필요합니다. 즉, 정상 동작할 때는 몰라도 되지만 **문제가 생겼을 때 반드시 필요한 지식**입니다.

---

### 토픽 18. Fast DDS vs CycloneDDS — 어떻게 선택해야 할까?
ROS 2는 여러 DDS 구현체를 지원합니다.  
- 구현체 선택이 로봇 동작에 실제로 영향을 줄 수 있을까요?
- 처음 배우는 사람은 어떤 구현체를 쓰는 것이 좋을까요?

> **💬 보편적 답변**  
> DDS 구현체 선택은 **네트워크 성능, 지연 시간, 멀티캐스트 지원** 측면에서 실제 차이를 만듭니다. 일반적으로 CycloneDDS는 낮은 지연 시간과 안정성으로 많이 선택되며, Fast DDS는 기본 구현체로 보안 기능이 풍부합니다. 입문자는 **ROS 2 기본 제공 구현체(Fast DDS)를 그대로 사용**하다가, 실제 성능 문제가 생기거나 특수한 요구가 있을 때 변경을 고려하는 것이 현실적입니다.

---

### 토픽 19. Discovery 과정에서 발생하는 문제들
DDS의 자동 노드 발견(Discovery)은 편리하지만 문제가 생기기도 합니다.  
- 노드가 분명히 실행 중인데 서로를 발견하지 못하는 상황이 왜 발생할까요?
- 이런 문제를 해결하기 위한 방법에는 어떤 것들이 있을까요?

> **💬 보편적 답변**  
> 자동 발견 실패의 주요 원인은 **방화벽·UDP 포트 차단, 다른 서브넷, 멀티캐스트 미지원 네트워크** 등입니다. WSL2나 Docker 환경에서 특히 자주 발생합니다. 해결 방법으로는 ① 방화벽에서 DDS 사용 포트(7400번대 UDP) 허용 ② DDS Discovery Server 설정 ③ Static Discovery 직접 지정 ④ 같은 서브넷 내로 네트워크 구성 등이 있습니다. 입문 단계에서는 같은 PC 내 로컬 통신부터 시작하고, 멀티 PC 통신은 단계적으로 시도하는 것이 좋습니다.

---

## 8장: DDS의 QoS

### 토픽 20. QoS를 잘못 설정하면 어떤 일이 벌어지나?
QoS 불일치는 토픽 연결 실패로 이어집니다.  
- 퍼블리셔와 서브스크라이버 QoS가 맞지 않아 데이터가 전달되지 않는 상황을 경험해 보셨나요?
- 이런 문제를 사전에 예방하는 방법은 무엇일까요?

> **💬 보편적 답변**  
> 가장 흔한 실수는 퍼블리셔는 `BEST_EFFORT`, 서브스크라이버는 `RELIABLE`로 설정하는 경우입니다. 이때 두 노드 모두 정상 실행되는 것처럼 보이지만 데이터는 전혀 수신되지 않습니다. 예방 방법은 ① `ros2 topic info --verbose`로 QoS 정보를 항상 확인하는 습관 ② 팀 내 토픽 QoS 표준을 정해두기 ③ 커스텀 QoS 보다 ROS 2의 **사전 정의된 프로파일**(sensor_data, parameters 등)을 우선 활용하는 것입니다.

---

### 토픽 21. Reliability RELIABLE vs BEST_EFFORT — 무조건 RELIABLE이 좋은가?
RELIABLE은 전달을 보장하지만, BEST_EFFORT보다 오버헤드가 큽니다.  
- 카메라나 라이다 같은 고빈도 센서 데이터에 RELIABLE을 쓰면 어떤 문제가 생길까요?
- 반대로 BEST_EFFORT를 써야 할 때 RELIABLE을 쓰는 것이 위험한 상황은 무엇일까요?

> **💬 보편적 답변**  
> 카메라 이미지(30fps 이상)에 RELIABLE을 설정하면 **재전송 큐가 쌓여 지연이 증가**하고, 오히려 최신 이미지 대신 오래된 이미지가 처리될 수 있습니다. 실시간 시각 처리에서는 약간의 손실보다 최신 데이터가 훨씬 중요합니다. RELIABLE이 위험한 상황은 **네트워크가 불안정한 환경에서 재전송이 폭증**해 전체 시스템이 느려지는 경우입니다. 규칙: **"지금 이 순간의 값이 중요하면 BEST_EFFORT, 반드시 도착해야 하면 RELIABLE"**로 판단하면 됩니다.

---

### 토픽 22. Durability TRANSIENT_LOCAL은 언제 쓰는가?
늦게 구독한 노드가 이전 데이터를 받을 수 있는 기능입니다.  
- 어떤 종류의 데이터에 TRANSIENT_LOCAL이 필수적인가요?
- TRANSIENT_LOCAL을 잘못 사용하면 어떤 부작용이 생길 수 있나요?

> **💬 보편적 답변**  
> **지도(Map) 데이터, 로봇 초기 상태, 설정 파라미터** 같이 "한 번 발행되면 항상 유효한" 데이터에 TRANSIENT_LOCAL이 필수입니다. 예를 들어 SLAM 노드가 맵을 먼저 발행하고, 내비게이션 노드가 나중에 구독을 시작할 때 맵을 받을 수 있어야 합니다. 부작용으로는 **오래된 데이터가 의도치 않게 새 구독자에게 전달**되는 경우가 있습니다. 예: 이전 주행에서 발행된 비상정지 명령이 재시작 후 새 노드에 전달되는 문제. 따라서 동적으로 변하는 명령·이벤트에는 VOLATILE이 안전합니다.

---

### 토픽 23. QoS를 잘 활용한 실제 시나리오를 설계해본다면?
지금까지 배운 QoS 개념을 실제 로봇 시스템에 적용해 봅시다.  
- 자율주행 배달 로봇이 있다고 가정할 때, 각 토픽에 어떤 QoS를 적용하겠나요?
- QoS 설정이 잘못되어 사고가 날 수 있는 시나리오를 상상해 볼 수 있나요?

> **💬 보편적 답변**  
> 자율주행 배달 로봇 예시:  
> - `/camera/image_raw`: BEST_EFFORT + VOLATILE (고빈도, 최신 이미지만 필요)  
> - `/emergency_stop`: RELIABLE + VOLATILE (반드시 전달, 오래된 명령은 불필요)  
> - `/map`: RELIABLE + TRANSIENT_LOCAL (늦게 합류한 노드도 맵 수신 필수)  
> - `/odom`: BEST_EFFORT + VOLATILE (센서 스트림, 일부 손실 허용)  
>
> 사고 시나리오: 비상정지 명령 토픽에 BEST_EFFORT를 설정했을 때 네트워크 혼잡으로 명령이 누락되면 로봇이 장애물을 향해 계속 주행하는 상황이 발생할 수 있습니다. QoS 선택이 안전과 직결됩니다.

---

## 9장: ROS 프로그래밍 규칙(코드 스타일)

### 토픽 24. 코드 스타일 통일이 정말 생산성을 높일까?
PEP 8, clang-format, 네이밍 규칙 같은 스타일 가이드를 따르는 이유를 생각해 봅시다.  
- "동작만 하면 된다"는 관점과 "읽히는 코드가 우선"이라는 관점 중 어디에 더 동의하나요?
- 팀 프로젝트에서 스타일 불일치가 실제로 어떤 비용을 만들까요?

> **💬 보편적 답변**  
> 코드 스타일은 성능을 직접 높이지는 않지만, 리뷰 속도와 유지보수 효율을 크게 높입니다. 특히 ROS 2처럼 노드가 많은 프로젝트에서는 파일 간 이동이 잦아 일관된 네이밍·포맷이 중요합니다. 스타일이 제각각이면 버그보다 "코드 해석"에 시간이 더 들고, 신규 인원의 온보딩도 느려집니다.

---

### 토픽 25. 자동 포맷터에 어디까지 맡길 수 있을까?
Python의 `black`, C++의 `clang-format`을 적용하면 코드가 자동 정렬됩니다.  
- 포맷터를 강제하면 개발자 개성이 사라진다는 의견에 대해 어떻게 생각하나요?
- 포맷터가 해결하지 못하는 코드 품질 문제는 무엇일까요?

> **💬 보편적 답변**  
> 포맷터는 "형식"을 통일해 논쟁을 줄이는 도구이며, 개성보다 협업 효율을 우선합니다. 다만 포맷터만으로는 함수 분리, 책임 분배, 예외 처리 전략 같은 구조적 품질을 보장할 수 없습니다. 따라서 자동 포맷 + 리뷰 체크리스트(가독성, 테스트 가능성, 예외 안전성)를 함께 운영하는 것이 효과적입니다.

---

## 10장: ROS 프로그래밍 기초(파이썬)

### 토픽 26. rclpy로 시작할 때 가장 중요한 습관은?
파이썬 노드는 빠르게 만들 수 있지만, 구조가 무너지기 쉽습니다.  
- 초보자가 처음부터 지켜야 할 최소한의 구조 규칙은 무엇일까요?
- "작동하는 스크립트"와 "유지보수 가능한 노드"의 차이는 어디서 갈릴까요?

> **💬 보편적 답변**  
> 최소한 지켜야 할 습관은 ① `Node` 클래스 분리 ② 콜백은 짧게 유지 ③ 로직 함수와 ROS I/O 분리입니다. 데모 단계에서는 스크립트 한 파일도 가능하지만, 기능이 늘면 콜백 안에 비즈니스 로직이 쌓여 디버깅이 어려워집니다. 초기에 구조를 분리해 두면 테스트 코드 작성도 쉬워집니다.

---

### 토픽 27. Python 노드의 성능 한계, 어디까지 허용할 수 있을까?
빠른 개발과 높은 성능은 종종 충돌합니다.  
- 어떤 경우에는 Python으로 충분하고, 어떤 경우에는 C++ 전환이 필요할까요?
- 멀티스레딩/멀티프로세싱을 적용할 때 주의할 점은 무엇일까요?

> **💬 보편적 답변**  
> 센서 전처리, 제어 정책 프로토타이핑, 오케스트레이션 노드는 Python으로 충분한 경우가 많습니다. 반면 고주기 제어(예: 1kHz)나 대용량 포인트클라우드 처리처럼 지연 민감한 작업은 C++가 유리합니다. Python에서 성능을 높이려면 GIL 영향, 콜백 블로킹, 프로세스 간 직렬화 비용을 함께 고려해야 합니다.

---

## 11장: ROS 프로그래밍 기초(C++)

### 토픽 28. rclcpp 학습의 진입장벽은 문법인가, 구조인가?
C++는 문법 자체도 어렵지만 ROS 2 구조도 복잡합니다.  
- 초보자에게 가장 어려운 요소는 스마트 포인터, 템플릿, 빌드 시스템 중 무엇이라고 보나요?
- C++ 노드를 처음 설계할 때 피해야 할 대표 실수는 무엇일까요?

> **💬 보편적 답변**  
> 실제로는 문법보다 "메모리/생명주기/빌드"를 함께 이해해야 하는 구조적 난도가 큽니다. 흔한 실수는 콜백에서 무거운 연산을 수행해 Executor를 막거나, 소유권이 불명확한 포인터 사용으로 상태를 꼬이게 만드는 것입니다. `shared_ptr` 사용 규칙과 클래스 책임 범위를 초기에 합의하면 문제를 크게 줄일 수 있습니다.

---

### 토픽 29. CMakeLists.txt와 package.xml, 어느 쪽이 더 중요할까?
ROS 2 C++ 패키지는 빌드 설정과 메타정보가 모두 필요합니다.  
- 의존성 누락 문제는 왜 자주 발생할까요?
- "빌드는 되는데 런타임에서 실패"하는 상황을 어떻게 줄일 수 있을까요?

> **💬 보편적 답변**  
> `CMakeLists.txt`는 컴파일/링크를, `package.xml`은 의존성 메타정보를 담당하므로 둘 다 필수입니다. 한쪽만 수정하면 환경에 따라 "내 PC에서는 되는데 다른 PC에서는 안 되는" 문제가 발생합니다. 예방책은 의존성을 두 파일에 동시에 반영하고, 깨끗한 워크스페이스에서 `colcon build --symlink-install` 재검증하는 것입니다.

---

## 12장: ROS 2 Tips

### 토픽 30. 디버깅은 CLI 중심이 좋은가, GUI 중심이 좋은가?
`ros2 topic`, `ros2 node`, `ros2 doctor` 같은 CLI와 RViz2/rqt 같은 GUI를 모두 사용할 수 있습니다.  
- 문제 해결 속도를 높이는 개인 루틴이 있나요?
- 초보자에게는 어떤 순서의 진단 절차를 추천하나요?

> **💬 보편적 답변**  
> 실전에서는 CLI로 상태를 빠르게 확인하고, GUI로 구조를 시각 검증하는 혼합 방식이 가장 효율적입니다. 추천 순서는 ① 노드 존재 확인(`ros2 node list`) ② 토픽 연결 확인(`ros2 topic list/info`) ③ 데이터 확인(`ros2 topic echo`) ④ 시각화 검증(RViz2)입니다. 이 순서를 습관화하면 원인 추적 시간이 크게 단축됩니다.

---

### 토픽 31. 로그와 파라미터 관리는 언제부터 체계화해야 할까?
초기에는 하드코딩이 빠르지만, 프로젝트가 커지면 운영 난도가 증가합니다.  
- 개발 초기에 파라미터 파일(YAML)을 도입하는 것이 과한가요?
- 로그 레벨 전략(debug/info/warn/error)은 팀 단위로 표준화해야 할까요?

> **💬 보편적 답변**  
> 작은 예제에서는 하드코딩이 빠르지만, 2개 이상의 실행 환경(시뮬레이션/실기기)이 생기면 파라미터 분리가 필수에 가깝습니다. 로그도 팀 규칙 없이 누적되면 중요한 경고가 묻히므로 레벨 사용 기준을 정하는 것이 좋습니다. "재현 가능한 실행 설정"과 "읽히는 로그"는 디버깅 비용을 줄이는 핵심 자산입니다.

---

## 13장: 토픽, 서비스, 액션 인터페이스

### 토픽 32. 인터페이스 설계에서 '확장성'을 어떻게 담보할까?
메시지 타입을 처음 설계할 때 미래 요구사항을 예측하기 어렵습니다.  
- 너무 단순한 메시지와 너무 복잡한 메시지 중 어떤 위험이 더 클까요?
- 인터페이스 변경이 불가피할 때 팀은 어떤 원칙으로 버전 관리를 해야 할까요?

> **💬 보편적 답변**  
> 과도한 일반화는 사용성을 떨어뜨리고, 과도한 단순화는 잦은 breaking change를 부릅니다. 실무에서는 "현재 요구 + 가까운 확장"만 반영하고, 의미 단위가 분명한 필드명/단위를 명시하는 것이 안정적입니다. 변경이 필요하면 메시지 버전 분리(`v1`, `v2`)와 호환 기간 운영 정책을 함께 두는 것이 안전합니다.

---

### 토픽 33. Topic/Service/Action 경계를 잘못 잡으면 어떤 일이 생길까?
인터페이스 선택은 시스템 응답성과 복잡도에 직접 영향을 줍니다.  
- 장시간 작업을 Service로 구현했을 때의 운영 리스크는 무엇일까요?
- 반대로 단순 요청까지 Action으로 만들면 어떤 비용이 발생할까요?

> **💬 보편적 답변**  
> 장시간 작업을 Service로 구현하면 타임아웃, 재시도, 취소 처리 로직을 애플리케이션에서 직접 떠안아야 해 장애 대응이 어려워집니다. 반면 단순 조회/설정까지 Action으로 만들면 상태 머신 관리와 코드 복잡도가 불필요하게 증가합니다. 핵심은 "지속 스트림=Topic, 단발 요청=Service, 장기 작업+피드백=Action"의 경계를 팀 규칙으로 명문화하는 것입니다.

---

## 14장: ROS 2 패키지 설계 (Python)

### 토픽 34. 노드를 4개로 분리한 설계, 적절한가?
예제에서는 `argument_node`, `operator_node`, `calculator_node`, `checker_node`로 역할을 분리합니다.  
- 이렇게 노드를 분리하면 어떤 이점이 있을까요?
- 반대로 하나의 노드로 통합하면 어떤 문제가 생길까요?

> **💬 보편적 답변**  
> 노드를 기능 단위로 분리하면 각 노드를 독립적으로 교체·재시작할 수 있고, 문제 발생 시 원인을 좁히기 쉬워집니다. 예를 들어 `operator_node`(연산)가 오동작해도 `argument_node`(데이터 수집)는 계속 동작합니다. 반면 하나로 통합하면 노드 간 통신 오버헤드가 줄지만, 코드 복잡도가 증가하고 일부 기능만 교체하거나 재시작하기 어려워집니다. 실제 로봇 시스템에서는 "독립적으로 재시작할 수 있는 단위인가?"를 분리 기준으로 삼는 것이 실용적입니다.

---

### 토픽 35. `setup.py`의 `data_files`와 `entry_points`, 헷갈린 경험이 있나요?
`data_files`는 설치 파일 경로를, `entry_points`는 노드 실행 진입점을 정의합니다.  
- 이 두 설정을 빠뜨렸을 때 어떤 오류가 발생하나요?
- ROS 2 Python 패키지와 일반 Python 패키지의 `setup.py` 차이는 무엇일까요?

> **💬 보편적 답변**  
> `entry_points`를 누락하면 `ros2 run` 시 "No executable found" 오류가 발생합니다. `data_files`를 누락하면 런치 파일이나 YAML 파라미터 파일이 `install` 디렉터리에 복사되지 않아 런치 시 파일을 찾지 못하는 오류가 납니다. 일반 Python 패키지는 `data_files`에 ROS 2 패키지 경로(`share/<name>/`)를 명시할 필요가 없지만, ROS 2는 설치 경로 규약이 있어 이를 반드시 따라야 `ros2 launch`에서 정상 참조됩니다.

---

## 15장: ROS 2 토픽 프로그래밍 (Python)

### 토픽 36. `MultiThreadedExecutor`와 `MultiThreadedCallbackGroup`, 언제 필요한가?
기본 실행자는 단일 스레드로 동작하지만, 복잡한 노드에서는 병렬 처리가 필요합니다.  
- 콜백 함수 안에 오래 걸리는 작업을 넣으면 어떤 문제가 생길까요?
- `MultiThreadedCallbackGroup`을 쓰면서 주의해야 할 동시성 문제는 무엇일까요?

> **💬 보편적 답변**  
> 단일 스레드에서 콜백이 오래 걸리면 그 사이에 들어오는 다른 메시지나 서비스 요청이 처리되지 않아 시스템 전체가 응답 없이 멈출 수 있습니다. `MultiThreadedExecutor` + `MultiThreadedCallbackGroup`을 사용하면 병렬 처리가 가능하지만, 여러 콜백이 동시에 같은 멤버 변수에 접근하면 race condition이 발생할 수 있습니다. 공유 상태가 있는 경우 Lock을 걸거나, 설계 단계에서 상태를 단일 콜백에만 맡기는 방식으로 예방하는 것이 좋습니다.

---

### 토픽 37. QoS `depth`(큐 크기)는 어떻게 결정해야 할까?
`depth=10`으로 설정하면 최대 10개의 메시지가 큐에 쌓입니다.  
- 큐가 너무 작으면, 너무 크면 각각 어떤 문제가 생길까요?
- 센서 데이터와 제어 명령 토픽에 같은 `depth`를 쓰는 게 적절할까요?

> **💬 보편적 답변**  
> 큐가 너무 작으면 처리 속도가 수신 속도보다 느릴 때 오래된 메시지부터 버려지고, 너무 크면 지연된 데이터가 쌓여 처리 시점에 "과거의 값"을 사용하게 됩니다. 센서 데이터처럼 "최신 값이 중요한" 경우는 `depth=1~5`로 작게, 제어 명령처럼 "모든 메시지를 빠짐없이 처리해야 하는" 경우는 크게 설정하는 것이 일반적입니다. 결국 depth는 발행 주기와 처리 시간을 측정한 뒤 결정하는 것이 가장 정확합니다.

---

## 16장: ROS 2 서비스 프로그래밍 (Python)

### 토픽 38. 서비스와 파라미터의 차이, 실제 코드에서 느껴지나요?
서비스는 사용자가 인터페이스를 정의하지만, 파라미터는 ROS 2의 기존 인터페이스를 사용합니다.  
- 같은 값을 설정할 때 서비스로 구현하는 것과 파라미터로 구현하는 것의 차이는 무엇일까요?
- 어떤 상황에서 파라미터 대신 서비스 커스텀 인터페이스를 사용하는 것이 적합할까요?

> **💬 보편적 답변**  
> 단순한 값 설정·조회라면 파라미터(`ros2 param set`)가 더 편리하고 CLI 도구와 YAML 파일이 자동 지원됩니다. 반면 "두 수를 받아 연산 결과를 돌려주는" 것처럼 **입력과 출력이 모두 필요한 비즈니스 로직**은 커스텀 서비스 인터페이스가 적합합니다. 기준은 "이 기능이 값 저장인가(파라미터), 계산 또는 동작 실행인가(서비스)"로 판단하면 됩니다.

---

### 토픽 39. `call_async`와 `spin_once`의 조합이 왜 필요한가?
비동기 서비스 호출 후 `spin_once`를 호출해야 응답이 처리됩니다.  
- 비동기 방식이 동기 방식보다 구조적으로 복잡한 이유는 무엇일까요?
- 비동기 호출 없이 동기 방식만 사용하면 어떤 상황에서 시스템이 멈출 수 있나요?

> **💬 보편적 답변**  
> 동기 방식(`call_sync`)은 응답이 올 때까지 코드 실행이 멈추기 때문에, 그 사이에 들어오는 다른 토픽이나 타이머 콜백을 처리하지 못합니다. 하나의 서비스 응답이 1초 이상 걸리면 그 노드 전체가 1초간 멈추는 셈입니다. `call_async` + `spin_once`는 "요청은 보내되, ROS 이벤트 루프를 계속 돌리면서 응답을 기다리는" 구조로 이 문제를 해결합니다. 처음에는 복잡해 보이지만 복수의 서비스를 사용하거나 타이머와 함께 쓸 때 그 가치가 분명해집니다.

---

## 17장: ROS 2 액션 프로그래밍 (Python)

### 토픽 40. 피드백(Feedback)이 필요 없다면 굳이 액션을 써야 할까?
액션은 피드백과 취소 기능이 핵심인데, 이를 사용하지 않는다면 서비스와 차이가 없어 보입니다.  
- 피드백 없이 완료까지 오랜 시간이 걸리는 작업에는 서비스와 액션 중 무엇이 더 적합할까요?
- 액션의 "취소(cancel)" 기능이 실제로 중요한 시나리오를 생각해볼 수 있나요?

> **💬 보편적 답변**  
> 피드백이 없더라도 작업 완료까지 10초 이상 걸린다면 서비스보다 액션을 권장합니다. 서비스는 타임아웃을 직접 처리해야 하고, 중간에 취소도 불가능하기 때문입니다. 취소 기능이 중요한 실제 시나리오는 자율주행 중 사람이 갑자기 경로에 등장해 이동 목표를 즉시 중단해야 하는 상황, 또는 사용자가 UI에서 "정지" 버튼을 누를 때입니다. 이런 경우 액션의 `cancel_goal`이 없으면 잘못된 동작이 계속 수행될 수 있습니다.

---

### 토픽 41. `GoalHandle`을 통한 상태 관리, 실수하기 쉬운 부분은?
액션 서버는 `GoalHandle`로 실행 중·성공·실패·취소 상태를 관리합니다.  
- `goal_handle.succeed()` 호출을 빠뜨리면 클라이언트 쪽에서 어떤 일이 벌어질까요?
- 액션 서버에서 예외가 발생했을 때 안전하게 처리하는 방법은 무엇일까요?

> **💬 보편적 답변**  
> `goal_handle.succeed()`를 호출하지 않으면 클라이언트의 결과 콜백이 영원히 호출되지 않아 클라이언트 노드가 결과를 기다리며 멈추게 됩니다. 예외 발생 시에는 `try-except`로 잡아 `goal_handle.abort()`를 명시적으로 호출해 클라이언트에 실패를 알려야 합니다. 특히 while 루프 내부에서 예외가 터지면 `succeed()`나 `abort()` 호출이 누락되기 쉬우므로, `finally` 블록에서 상태를 확정하는 패턴이 안전합니다.

---

## 18장: ROS 2 파라미터 프로그래밍 (Python)

### 토픽 42. `add_on_set_parameter_callback`으로 검증하는 것, 어디까지가 적절한가?
파라미터 변경 콜백에서 범위 검증, 타입 확인 등을 수행할 수 있습니다.  
- 파라미터 변경을 "거부"하는 로직을 어느 정도까지 넣어야 할까요?
- 너무 엄격한 검증이 오히려 운영을 방해하는 경우도 있을까요?

> **💬 보편적 답변**  
> 최소한 **타입 검증**(정수인지, 문자열인지)과 **범위 검증**(음수 불허, 최댓값 초과 불허)은 반드시 넣는 것이 안전합니다. 하지만 지나치게 엄격하면 긴급 상황에서 파라미터를 빠르게 조정해야 할 때 오히려 방해가 됩니다. 운영 모드와 디버그 모드를 분리해, 디버그 시에는 검증을 완화하는 플래그를 두는 방식도 실용적입니다. 결국 "이 값이 잘못 설정되면 로봇이 위험한가?"를 기준으로 검증 엄격도를 결정해야 합니다.

---

### 토픽 43. YAML 파일로 파라미터를 관리할 때 가장 큰 실수는?
YAML 파일로 초기화하는 방식은 편리하지만 함정도 있습니다.  
- CLI로 파라미터를 변경한 뒤 노드를 재시작하면 어떤 일이 벌어질까요?
- `/**` 와일드카드를 쓰는 것이 항상 좋은 선택일까요?

> **💬 보편적 답변**  
> CLI(`ros2 param set`)로 변경한 값은 메모리에만 저장되어 노드 재시작 시 YAML 파일의 원래 값으로 돌아옵니다. 이 사실을 모르면 "분명히 바꿨는데 왜 또 원래 값이야?"라는 혼란이 반복됩니다. 장기 설정 변경은 YAML 파일 자체를 수정해야 합니다. `/**` 와일드카드는 노드 이름 변경에 유연하지만, 여러 노드가 같은 이름의 파라미터를 사용할 때 의도치 않은 노드에도 적용될 수 있습니다. 노드가 많아지면 명시적 노드 이름으로 범위를 한정하는 것이 안전합니다.

---

## 19장: ROS 2 실행 인자 프로그래밍 (Python)

### 토픽 44. 파라미터(Parameter)와 실행 인자(Argument)는 어떻게 다른가?
둘 다 노드 동작을 외부에서 변경하는 수단이지만 쓰임새가 다릅니다.  
- 어떤 값은 파라미터로, 어떤 값은 실행 인자로 전달하는 것이 적합할까요?
- 런타임 중 변경이 필요 없는 값에 파라미터를 쓰는 것이 낭비일까요?

> **💬 보편적 답변**  
> **실행 인자**는 노드를 시작할 때 딱 한 번 설정되고 이후 변경되지 않는 값(예: 목표 합계값, 모드 플래그)에 적합합니다. **파라미터**는 런타임 중에도 `ros2 param set`으로 동적 변경이 필요한 값에 적합합니다. 런타임 변경이 없는 값에 파라미터를 쓰는 건 기능적으로는 동작하지만, `declare_parameter`·콜백 등 코드가 복잡해지는 불필요한 오버헤드가 생깁니다. "노드 실행 후에도 바꿀 필요가 있는가?"로 구분하면 됩니다.

---

### 토픽 45. `argparse`와 `rclpy.init(args=)`를 함께 쓸 때 주의할 점은?
ROS 2 관련 인자와 사용자 정의 인자를 분리해야 합니다.  
- `sys.argv`를 그대로 `argparse`에 넘기면 어떤 문제가 생길 수 있나요?
- `argparse`가 인식하지 못한 인자를 `rclpy.init()`에 전달하는 패턴이 왜 중요할까요?

> **💬 보편적 답변**  
> ROS 2 노드는 `__node:=이름`처럼 ROS 전용 인자를 받는데, `argparse`가 이 인자를 만나면 "인식할 수 없는 인자" 오류를 냅니다. 이를 방지하려면 `parse_known_args()`를 사용해 `argparse`가 처리하는 인자와 나머지를 분리한 뒤, 나머지를 `rclpy.init(args=remaining)`에 넘겨야 합니다. 이렇게 하면 `ros2 run`의 remapping 기능(`__node`, `__ns`)과 사용자 정의 인자가 충돌 없이 공존할 수 있습니다.

---

### 토픽 46. 실행 인자, 파라미터, 런치 파일 — 무엇을 선택해야 할까?
같은 목적을 세 가지 방법으로 달성할 수 있습니다.  
- 팀 프로젝트에서 세 방법을 혼용하면 어떤 혼란이 생길까요?
- 규모가 커질수록 어떤 방법으로 수렴하게 될까요?

> **💬 보편적 답변**  
> 세 방법을 목적에 맞게 역할 분리하면 됩니다. **실행 인자**는 간단한 일회성 실험이나 스크립트 실행 시, **파라미터 + YAML 파일**은 설정값이 많거나 런타임 변경이 필요한 경우, **런치 파일**은 여러 노드를 한 번에 구성하고 환경별(실기/시뮬)로 다르게 실행해야 할 때 사용합니다. 규모가 커질수록 런치 파일 + YAML 파일 조합으로 수렴하는 경향이 있으며, 하드코딩과 실행 인자는 프로토타이핑 단계에만 남기는 것이 유지보수에 유리합니다.

---

## 💡 토론 진행 가이드

| 항목 | 내용 |
|------|------|
| 토론 방식 | 토픽별 자유 발언 → 반대 의견 → 정리 |
| 시간 배분 | 토픽당 5~10분, 전체 60~90분 |
| 진행자 역할 | 토픽 소개, 발언 유도, 시간 관리 |
| 참고 | 보편적 답변은 참고용이며, 다양한 관점 존중 |
